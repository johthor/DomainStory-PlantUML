' Actors
' ##################################

' Person Actor
' ==================================

!global $Person_IconStyle ?= $Actor_IconStyle

!if $Person_IconStyle == "stickman"
    !$Person_Shape ?= "Actor"
    $extendStyleDeclarations("Actor", "Person", "")
    skinparam ActorStyle stickman
!elseif $Person_IconStyle == "stickmanAlt"
    !$Person_Shape ?= "Actor/"
    $extendStyleDeclarations("Actor", "Person", "")
    skinparam ActorStyle stickman
!elseif $Person_IconStyle == "hollow"
    !$Person_Shape ?= "Actor"
    $extendStyleDeclarations("Actor", "Person", "")
    skinparam ActorStyle hollow
!elseif $Person_IconStyle == "awesome"
    !$Person_Shape ?= "Actor"
    $extendStyleDeclarations("Actor", "Person", "")
    skinparam ActorStyle awesome
!elseif $Person_IconStyle == "outline"
    $extendStyleDeclarations("Actor", "Person", "account_outline")
!else
    $extendStyleDeclarations("Actor", "Person", "account")
!endif

!unquoted function Person($label, $tag = "", $note = "", $shape = "", $scale = "", $color = "", $background = "")
    !return Actor("Person", $Person_IconName, $label, $tag, $note, $shape, $scale, $color, $background)
!endfunction

!unquoted procedure namedPerson($name, $label = "", $tag = "", $note = "", $shape = "", $scale = "", $color = "", $background = "")
    !if $label == ""
        !$label = $name
    !endif
    introduce($name, Person($label, $tag, $note, $shape, $scale, $color, $background))
!endfunction

' Group Actor
' ==================================

!global $Group_IconStyle ?= $Actor_IconStyle

!if $Group_IconStyle == "outline"
    $extendStyleDeclarations("Actor", "Group", "account_multiple_outline")
!else
    $extendStyleDeclarations("Actor", "Group", "account_multiple")
!endif

!unquoted function Group($label, $tag = "", $note = "", $shape = "", $scale = "", $color = "", $background = "")
    !return Actor("Group", $Group_IconName, $label, $tag, $note, $shape, $scale, $color, $background)
!endfunction

!unquoted procedure namedGroup($name, $label = "", $tag = "", $note = "", $shape = "", $scale = "", $color = "", $background = "")
    !if $label == ""
        !$label = $name
    !endif
    introduce($name, Group($label, $tag, $note, $shape, $scale, $color, $background))
!endfunction

' System Actor
' ==================================

$extendStyleDeclarations("Actor", "System", "laptop")

!unquoted function System($label, $tag = "", $note = "", $shape = "", $scale = "", $color = "", $background = "")
    !return Actor("System", $System_IconName, $label, $tag, $note, $shape, $scale, $color, $background)
!endfunction

!unquoted procedure namedSystem($name, $label = "", $tag = "", $note = "", $shape = "", $scale = "", $color = "", $background = "")
    !if $label == ""
        !$label = $name
    !endif
    introduce($name, System($label, $tag, $note, $shape, $scale, $color, $background))
!endfunction
